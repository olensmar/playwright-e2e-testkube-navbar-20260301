# Playwright E2E tests for Testkube navbar

This repository contains Playwright end-to-end tests that validate the main navbar of [testkube.io](https://testkube.io).

## Getting started

```bash
npm install
npx playwright install --with-deps
npm test
```

The default configuration runs tests against `https://testkube.io` on multiple desktop and mobile browsers.
